# IndustrialDAQ UI 说明文档

本文件旨在提供 IndustrialDAQ WPF 客户端界面的结构、功能和设计规范的详细说明。

## 1. 总体架构

- **技术栈**: WPF (.NET) + Prism (MVVM 框架)
- **视觉风格**: 现代、简洁的工业风格。采用 **无边框窗口 (Frameless Window)** 设计，支持 **深色 (Dark)** 和 **浅色 (Light)** 主题切换。
- **布局模式**: 侧边导航栏 + 主内容区域 + 全局状态栏 + 悬浮通知系统。

## 2. 核心界面组件

### 2.1 主窗口 (Shell - MainWindow)
- **自定义标题栏**: 包含应用图标、标题以及自定义的最小化、最大化和关闭按钮。支持鼠标拖动窗口。
- **侧边导航栏**: 提供核心功能的快速切换，包含图标、文本以及报警数量角标（针对报警日志项）。
- **主内容区 (MainRegion)**: 基于 Prism Region 管理，动态加载不同的业务视图。
- **全局状态栏**: 
  - **连接状态**: 实时显示与数据采集引擎的连接状态（绿色表示在线，红色表示断开）。
  - **状态消息**: 显示系统当前的运行状态或最近的操作提示。
  - **页面定位**: 显示当前所处的功能页面名称。
- **通知系统**: 右下角弹出式 Toast 通知，用于展示系统告警、操作成功/失败等实时反馈。

### 2.2 核心视图

#### 📈 首页概览 (DashboardView)
- **数据汇总卡片**: 实时展示“总产量”、“良品率”、“总能耗”等关键 KPI 指标，配有直观的图标和颜色区分。
- **系统状态**: 集中展示当前用户、系统运行时间和活跃报警总数。
- **实时趋势图**: 集成 `LiveChartsCore` 展示产线生产波动的实时走势。

#### 🏭 设备监控 (ProductionMonitorView)
- **设备切换**: 通过下拉列表快速选择关注的生产设备。
- **连接详情**: 展示当前选中设备的 IP 地址、端口号及使用的协议驱动类型。
- **可视化仪表盘**: 使用自定义的 `SemiCircleGauge`（半圆压力表）实时显示设备的关键物理量（如温度、压力、速度等）。

#### 🌳 测点详情 (DeviceDetailView)
- **资源树结构**: 采用层级化的树状列表展示“区域 -> 设备 -> 测点”的物理拓扑。
- **对齐网格**: 虽然是树形结构，但通过 SharedSizeGroup 实现了测点名称、实时值、数据质量和更新时间的完美对齐。
- **交互操作**: 提供对单个测点的“写入”、“趋势查看”等操作入口。

#### 🔔 报警日志 (AlarmRecordView)
- **统计摘要**: 展示“报警总数”、“活跃报警数”及“报警确认率”。
- **多维筛选**: 支持按报警级别（严重、警告、信息）和处理状态（已激活、已确认、已清除）进行筛选。
- **列表管理**: 分页展示详细的报警触发时间、消息内容及恢复时间。

#### 📉 趋势监控 (TrendView)
- **历史回溯**: 支持查看特定时间段内的测点数值变化趋势。
- **多曲线对比**: 允许在同一图表中同时叠加多个测点的曲线进行关联分析。

#### 📋 模板与配置 (DeviceTemplateView & ResourceRuleConfigView)
- **设备建模**: 管理设备模板，定义数据点映射关系，支持模板的克隆和导出。
- **规则引擎配置**: 可视化配置报警触发规则和逻辑判定。

## 3. 自定义控件与样式

### 3.1 核心控件
- **SemiCircleGauge**: 专为工业场景设计的半圆仪表盘，支持自定义量程、颜色分段和动画效果。
- **NavRadioButton**: 定制的导航单选按钮，包含选中状态的高亮效果。

### 3.2 主题资源
- **DarkTheme.xaml**: 提供深蓝/黑色调的夜间工作模式，降低视觉疲劳。
- **LightTheme.xaml**: 提供高对比度的明亮模式，适合光线充足的环境。
- **资源定义**: 使用 `DynamicResource` 绑定颜色和画刷（如 `MainBgBrush`, `TextPrimaryBrush`, `InfoBrush` 等），确保主题切换时界面即时刷新。

## 4. 交互逻辑

- **导航**: 使用 `NavigateCommand` 配合 Prism 的 `RequestNavigate` 实现视图间的平滑切换。
- **对话框**: 采用 Prism 的 `IDialogService` 管理弹出式窗口（如登录、创建设备、写入数值等）。
- **实时性**: UI 通过绑定 ViewModels 中的可观察属性，配合底层数据总线，实现毫秒级的数据刷新感知。
